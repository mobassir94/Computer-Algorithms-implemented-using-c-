/*
problem : write a program that will take string as input(using pointer) and will print the string on the
 console(without using array)
 */
 #include<stdio.h>
 int main()
 {
    char *p,*q;
    p=gets(q);
    printf(p);

     return 0;
 }
